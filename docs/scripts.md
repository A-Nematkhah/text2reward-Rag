# Scripts

See also `scripts/README.md` in the repository root.

| Script | Purpose |
|--------|---------|
| `scripts/calibrate_smoke_gate.py` | Offline calibration of Stage B trajectory-bank gate thresholds |

Run from the repository root:

```bash
python scripts/calibrate_smoke_gate.py
```
